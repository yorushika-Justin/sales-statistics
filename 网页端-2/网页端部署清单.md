# 网页端部署清单 — 新 PC (song.jia.xin / Win11)

**生成时间**：2026-07-03
**目标机器**：N37805 / Windows 11 / Python 3.14.6
**依据文件**：`系统信息报告.txt`

---

## 一、环境信息（已确认）

| 项目 | 值 |
|------|-----|
| Python 路径 | `C:\Users\song.jia.xin\AppData\Local\Python\Pythoncore-3.14-64\Python.exe` |
| pythonw.exe | `C:\Users\song.jia.xin\AppData\Local\Python\Pythoncore-3.14-64\pythonw.exe` |
| 字体 | `C:\Windows\Fonts\msyh.ttc` ✅ 存在 |
| 输出目录 | `D:\销售汇总\` |
| 端口 | 5000 ✅ 可用 |

---

## 二、需要安装的依赖包

全部缺失，需逐个安装：

```powershell
# 方式1：一键安装（推荐）
pip install Flask xlrd openpyxl Pillow

# 方式2：逐个安装
pip install Flask
pip install xlrd
pip install openpyxl
pip install Pillow
```

| 包名 | 用途 | 安装命令 |
|------|------|----------|
| Flask | Web 框架 | `pip install Flask` |
| xlrd | 读 .xls 文件 | `pip install xlrd` |
| openpyxl | 读 .xlsx 文件 | `pip install openpyxl` |
| Pillow | 绘图（生成 PNG） | `pip install Pillow` |

**非必需（网页端可跳过）**：
- `tkinterdnd2` — GUI 拖拽窗口（exe方案用）
- `pyinstaller` — 打包exe用

---

## 三、需要复制的文件

从当前电脑复制到新 PC，保持目录结构：

```
目标位置：D:\Code\project10-销售统计\（或任意英文路径）

需复制的文件（共7个）：
├── 汇总脚本.py              ← 网页端核心依赖（sys.path 引用）
└── 网页端-2/
    ├── web_server.py        ← Flask 主程序
    ├── config.json          ← 配置文件（已修改为新PC路径）
    ├── index.html           ← 前端页面
    ├── 启动网页端.vbs        ← 静默启动器（推荐日常用）
    ├── 启动网页端.bat        ← 调试启动器（看日志用）
    └── 开机自启.bat          ← 开机自启管理脚本
```

---

## 四、完整安装步骤（AI 辅助执行）

### 步骤 1：安装依赖
```powershell
pip install Flask xlrd openpyxl Pillow
```

### 步骤 2：验证依赖
```powershell
python -c "import flask; import xlrd; import openpyxl; from PIL import Image; print('依赖安装成功')"
```

### 步骤 3：复制文件
将当前电脑的 `汇总脚本.py` 和整个 `网页端-2/` 文件夹复制到新 PC 的相同路径

### 步骤 4：创建输出目录
```powershell
mkdir D:\销售汇总
```
（程序也会自动创建）

### 步骤 5：启动测试
```powershell
# 方式1：静默启动（推荐）
双击 网页端-2\启动网页端.vbs

# 方式2：看日志启动（调试用）
python 网页端-2\web_server.py
```

### 步骤 6：功能验证
- 浏览器打开 `http://127.0.0.1:5000`
- 拖入一个 xls/xlsx 文件
- 检查是否生成 PNG 并自动打开

### 步骤 7：配置开机自启（可选）
```powershell
双击 网页端-2\开机自启.bat
# 按提示选择：1=注册 2=注销 3=查看状态
```

---

## 五、config.json 配置说明

已预配置为新 PC 路径：

```json
{
    "output_dir": "D:\\销售汇总\\",
    "font_path": "C:\\Windows\\Fonts\\msyh.ttc",
    "port": 5000
}
```

如需修改，编辑 `网页端-2/config.json` 后重启服务

---

## 六、注意事项

- **完全离线**：前端内嵌，无需联网
- **改代码后**：需重启 Flask 服务（非热加载）
- **端口被占**：改 config.json 的 port 或关闭占用程序

---

## 七、状态速查

| 检测项 | 状态 |
|--------|------|
| Python 3.14.6 | ✅ 已安装 |
| pythonw.exe | ✅ 路径确认 |
| 磁盘空间 | ✅ 充足（C盘212GB / D盘399GB） |
| 字体（微软雅黑） | ✅ 存在 |
| 端口 5000 | ✅ 可用 |
| 依赖包（4个） | ❌ 待安装 |
| 网页端文件（7个） | ❌ 待复制 |
